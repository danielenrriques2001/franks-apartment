---

import { getLangFromUrl, useTranslations } from '../utils/functions.js';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);


---

<footer class="footer">
    <p class="footer__para" set:html={t('footer')}></p>
</footer>
